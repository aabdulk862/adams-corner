{
  "version": 2,
  "builds": [{ "src": "./backend", "use": "@vercel/node" }],
  "routes": [
    { "src": "/menuItems/reset", "dest": "./backend/server.js" },
    { "src": "/(.*)", "dest": "./backend/server.js" }
  ]
}

